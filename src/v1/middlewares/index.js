const dns = require('./dns.js');
const verifyProtocol = require('./verifyProtocolo.js');
const validateResults = require('./validateResults');
const existShortURL = require('./existShortUrl');
module.exports = {
  dns,
  verifyProtocol,
  validateResults,
  existShortURL,
};
